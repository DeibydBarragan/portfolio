import Section from "../Section"

type Props = {}

export default function Contact({}: Props) {
  return (
    <Section
      className="bg-indigo-200 section"
      id="contact"
    >
      <h2>Contáctame</h2>
    </Section>
  )
}